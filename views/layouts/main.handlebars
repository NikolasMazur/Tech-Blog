<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/public/css/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">Tech Blog</a>
            </h1>
            <nav>
                {{#if logged_in}}
                <a href="/">Home</a>
                <a href="/dashboard">dashboard</a>
                <button id="signout" class="btn-no-style">signout</button>
                {{else}}
                <a href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
            <footer>
                <p>Nikolas Mazur</p>
            </footer>
    </div>

    {{#if logged_in}}
    <script src="/js/signout.js"></script>
    {{/if}}
</body>
</html>